import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Kandidaten · RR Recruitment Hub" };

export default function KandidatenPage() {
  return (
    <PlaceholderPage
      title="Kandidaten"
      description="Kandidatenbestand met status (in proces, beschikbaar, geplaatst, niet beschikbaar), huidige functie, bron en een verwijzing naar het cv. Een kandidaat kan op meerdere vacatures solliciteren."
    />
  );
}
